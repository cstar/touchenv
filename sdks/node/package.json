{
  "name": "@touchenv/node",
  "version": "0.1.0",
  "description": "touchenv SDK for Node.js — drop-in dotenv replacement",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test test/**/*.test.ts --loader tsx"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0"
  }
}
