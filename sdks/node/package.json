{
  "name": "@escapevelocityoperations/touchenv-node",
  "version": "0.1.3",
  "description": "touchenv SDK for Node.js — drop-in dotenv replacement",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && mkdir -p dist/bin && cp ../../cli/dist/bin/touchenv-keychain dist/bin/touchenv-keychain 2>/dev/null || true",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0"
  }
}
