{
  "name": "touchenv",
  "version": "0.1.0",
  "description": "Encrypted .env file manager",
  "type": "module",
  "bin": {
    "touchenv": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test --import tsx test/*.test.ts",
    "lint": "eslint src/ test/"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
